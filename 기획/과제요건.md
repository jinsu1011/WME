# 과제 요건 — AI 웹 서비스 설계 Mini-project

> 원본: 루트의 `AI캠퍼스_Full-stack Engineering_7. AI 웹 서비스 설계 Mini-project (1).pdf` (26쪽, 2026-09-14 수령)
> **이 문서가 제출물·평가에 대한 기준이다.** 이전 문서의 "3일차 13:00~14:00", "프로젝트 기술서 PDF·API 명세서·DB 다이어그램 PNG" 는 이 문서로 대체한다.
> 작성: HEADER, 2026-09-14

## 1. 무엇을 평가하나

**과정 범위는 "서비스 기획·요구사항 정의" + "시스템 설계·데이터 모델링" 까지다.**
어플리케이션 개발·테스트는 과정 범위 밖이다(2쪽). → **구현은 직접 점수가 아니다.**
단, 실제 화면은 UI 흐름도 캡처로, 실제 코드와 대조된 설계는 "UI·API·DB 불일치" 감점 회피로 쓴다.

## 2. 일정

| 날짜 | 가이드 |
|---|---|
| 1일차 (9/15) | 서비스 기획 — 서비스 개요, 요구사항 정의, UI 흐름도 |
| 2일차 (9/16) | 시스템 설계 — 데이터 모델, API 명세 |
| 3일차 (9/17 추정) | 기획·설계 보완, **13:30 제출 마감(미준수 0점)**, **14:30 산출물 발표 개인별 5분** |

## 3. 평가 기준 (100점)

| 항목 | 점수 | 내용 |
|---|---|---|
| 납기 | 20 | 3일차 13:30 마감. 미준수 0점 |
| 목표 시스템 정의 | 30 | **구현 및 기능 명확성(O), 창의·독창성(X)** — 서비스 개요·배경 / 액터별 시스템 기능(요구사항) / 전체 화면 와이어프레임 |
| 시스템 설계 — 데이터 모델 | 20 | API 를 제공하기 위해 필요한 모든 데이터 |
| 시스템 설계 — API 명세 | 20 | 화면 구현 시 호출되는 API 전체 목록(요청·응답·예외처리). **Security Schemes 제외** |
| 발표 | 10 | 전달 능력, **시간 엄수** |

### 세부 기준 (6쪽)

| 구분 | 반드시 포함 | 주의(감점) |
|---|---|---|
| 서비스 정의 | 서비스 목적·타깃 사용자, 구체적 문제 정의(원인·배경·현황), 논리적 기능 제안, 개요 ↔ 요구사항 일관성, 액터별 기능 ↔ 화면 연결 | 기능이 문제 해결과 연결 안 됨, **과도하게 큰 범위** |
| UI 흐름·화면 | 모든 기능이 UI 흐름에 반영, 화면 목록·사용자 흐름, 구성 요소(버튼·입력·데이터), 예외·오류 시나리오(가점), **UI ↔ API 필드 일치** | 핵심 기능이 UI 에 없음, **UI 데이터와 API 불일치** |
| 데이터 모델 | 모든 엔티티, **UI 의 모든 데이터 필드**, PK/FK·관계(1:N, M:N), 타입·제약조건, API 와 일관성 | **UI 데이터가 ERD 에 없음**, API 와 불일치 |
| API 명세 | 모든 기능 API, 화면별 필요 API, Request/Response ↔ ERD 일치, 적절한 HTTP Method, Path/Query/Body 명확, 에러 코드, 공통 스키마 재사용(`$ref`) | **UI 에서 필요한 API 누락**, API 와 데이터 모델 불일치 |

## 4. 제출물 — 파일명 반드시 준수

| 파일 | 형식 | 내용 | 원본·초안 |
|---|---|---|---|
| `{반_이름_프로젝트명}-개요.pdf` | PDF | 서비스 개요, 배경, 액터, **UI 흐름도(화면 캡처 필수)** | `기획/설계/서비스개요.md` + `기획/설계/화면설계.md` |
| `{반_이름_프로젝트명}-API.yml` | OpenAPI YAML | API 명세 | `제출/WME-API.yml` (초안) |
| `{반_이름_프로젝트명}-DB.dbml` | DBML | DB 다이어그램 | `제출/WME-DB.dbml` (초안) |

- 프로젝트명: **WME**. 반·이름: **미정 — 사용자 확인 필요.** 확정되면 초안 파일명을 바꾼다
- 정의서 목차(4쪽): 서비스 개요 / 시스템 액터 정의 / UI 흐름(와이어프레임) / 데이터 모델 설계 / API 명세 정의 / 고려사항
- 활용 도구(7쪽): UI 흐름도 — Balsamiq·PPT / 데이터 모델 — dbdiagram.io / API — Swagger Editor·OAS Editor
- 예시(15~25쪽): 회의실 예약 서비스 — 액터 표, 화면 캡처 + 빨간 점선 박스 + 화살표 + 동작 설명 흐름도, dbdiagram ERD, Swagger 화면

## 5. HEADER 결정 (2026-09-14, 과제 요건 대응)

사용자 지시: **3D 장비 뷰와 센서는 유지한다.** PPT(발표자료)는 나중에 정한다.

| # | 결정 | 이유 |
|---|---|---|
| R1 | 제출물을 **DBML · OpenAPI YAML · 개요 PDF** 로 만든다. 기존 `DB설계.md`·`API명세서.md` 는 원본 자료로 유지 | 파일 형식·이름이 평가 조건 |
| R2 | **3D·센서는 유지하되 "핵심 기능을 보여주는 화면"으로 문서화**한다. 센서는 **선택 입력 장치**, 키보드가 기본 | 기능 명확성 점수 + "과도한 범위" 감점 회피 |
| R3 | 센서 연결의 **예외 흐름**(미지원 브라우저·미연결·수신 없음·키보드 전환)을 UI 흐름도에 넣는다 | 세부 기준 "예외·오류 시나리오" 가점 |
| R4 | 센서 전용 API 가 없는 이유(Web Serial 브라우저 직통)와 실시간 기록(WebSocket)은 **YAML description + 고려사항**에 적는다 | OpenAPI 는 WebSocket 을 표현하지 않는다. 누락으로 보이지 않게 |
| R5 | **로그인 API·비밀번호 해시를 설계에 넣는다(`[설계]` 표시).** 시연 앱은 구현하지 않고 데모 전환 유지 | 로그인 화면에 대응 API 가 없으면 "UI 필요 API 누락". 평가는 설계이므로 정당. 구현했다고 쓰지 않는다 |
| R6 | **제출용 ERD(DBML)는 논리 설계로 정규화한다** — 시연 앱의 JSON 컬럼 10개에 든 화면 데이터를 테이블·컬럼으로 드러낸다(19테이블, 그룹 4개). M:N: 배정↔단계, 시도↔루브릭, 시도↔확인 항목, 피드백↔근거 구간. **API 응답 구조는 그대로**(배열 = 하위 테이블 행). 시연 앱 저장 위치는 각 테이블 Note·`화면설계.md` 4절에 매핑 | 데이터 모델 20점 기준 "UI 의 모든 데이터 필드 · PK/FK · 1:N·M:N · 타입·제약". JSON 한 칸이면 그림에서 필드가 안 보인다. *(9/14 밤 1차안 "note 로 풀기"를 사용자 위임으로 HEADER 가 변경)* |
| R7 | 액터는 **신입사원 · 매니저 2종 + 외부 시스템 2종(LLM, 교육용 모형 컨트롤러)** | 범위를 키우지 않는다. 과정 등록·배정은 범위 밖으로 명시 |
| R8 | 에러 코드는 **서버가 실제로 내는 것(404·409·422·503) + 설계 로그인의 401** 만 적는다 | 내지 않는 코드를 적으면 API 와 구현 불일치 |
| R9 | 3D·센서·LLM 에 **새 기능을 더하지 않는다.** 문서가 끝난 뒤에만 센서 측정 | 점수는 문서에서 나온다 |

## 5.4 9반 평가기준 — `웹 서비스 개발 미니프로젝트-9반, 평가기준.pdf` (2026-09-15 수령) ★ 이 절이 앞 절보다 우선

| 항목 | 9반 평가기준 | 앞선 기록과 달라진 점 |
|---|---|---|
| **마감** | **2026-09-17(목) 13:40**, 3종이 모두 올라간 시각으로 판정. 지연(당일) −5, 9/18 이후 −10, 하나라도 없으면 0 | 13:30 → **13:40** |
| **파일명** | **`{반}_{고유번호}_{이름}_{프로젝트명}-개요.pdf / -API.yml / -DB.dbml`** (예: `9반_P000_홍길동_회의실예약-개요.pdf`) | **고유번호가 추가됨** → **P286** 확정: `9반_P286_김진수_WME-*` |
| 제출 | 구글 드라이브 공유 폴더 업로드. 재업로드 가능, 마지막 파일이 최종본. 불가할 때만 실습교수 안내로 Slack DM | "있는 상태로 먼저 내고 보완해 재제출"이 유리하다고 명시 |
| 접수 요건 | 개요 PDF 안에 UI 흐름도 캡처 / yml 이 **Swagger UI 에서 오류 없이 파싱** / dbml 이 **dbdiagram.io 에서 오류 없이 렌더링** | 9/15 HEADER 확인 완료 |
| **주의** | **제출물에 실제 기업 정보나 개인정보를 포함하지 않는다** | 신규 — 데모 이름·고객사 표기 점검 필요 |
| 평가하지 않음 | 창의성·독창성, Security Schemes | 같음 |
| 감점 | 과도하게 큰 범위 | 같음. "기획서·화면·데이터·API 네 가지가 같은 이야기를 하면 잘 설계된 것" |
| **발표** | 1인 5분, **4분 30초~5분 마무리가 만점**(초과·2분 미만 감점). 전달 6 + 시간 4. 순서는 **고유번호 내림차순**. **발표 자료 = 프로젝트 기술서(개요 PDF) 중심, 별도 발표 자료 불필요**. 발표 중 코멘트 없음 | 발표용 자료 문제 해결 → 개요 PDF 로 발표 |
| 발표 구성 예시 | 어떤 문제를 푸는가 → 누가 무엇을(액터·기능) → 화면 흐름과 데이터·API 설계 → 고민한 점과 남은 과제 | 대본 구성 기준 |

## 5.5 참고자료 — 이전 기수 제출물 (2026-09-15 수령)

> 원본: `참고자료/` — PDF 31쪽(AI 3기 2반 "SKALA 교육플랫폼 과제제출&피드백 시스템") + `SKALA교육플랫폼-API.yml` + `SKALA교육플랫폼-DB.dbml`
> **참고만 한다. 따라 할 필요 없음 (사용자, 9/15).** 개요 PDF(슬라이드)는 **깔끔하게, 들어가야 할 내용만** 넣는다. 제작은 작업이 더 진행된 뒤 시작한다.
> 아래 표는 필요할 때 골라 쓰는 아이디어 목록이다.

| 항목 | 참고자료 | WME 에 적용 |
|---|---|---|
| 형식 | 가로 슬라이드 → PDF, 29쪽 | 같은 형식 (발표 겸용) |
| 목차 | 기획 배경 → Pain Points → 액터 → 전체 UI 흐름도 → 화면별 UI 흐름 → 데이터 모델 → API → Appendix | 과제 PDF 4쪽 목차에 맞춰 같은 순서. 고려사항은 Appendix 자리 |
| Pain Points | 액터별 "문제 → 해결" 3줄 카드 | 서비스개요 1.3 문제 1·2 ↔ 1.5 핵심 가치를 카드로 |
| 전체 UI 흐름도 | 액터별 화면 지도 한 장 (화면 이름 + 파일명) | 화면설계 2절 흐름을 신입사원·매니저 두 줄 지도로 |
| 화면별 UI 흐름 | 캡처 1~2장 + **범례: 초록 = 화면 이동 / 빨강 = API(메서드+경로)·기능 설명 / 파랑 = 모달·특이사항**. 오류 alert 도 캡처 | 같은 범례. 버튼·데이터 옆에 `GET /api/...` 를 빨강으로 적어 UI↔API 일치를 그림으로 보여 준다 |
| 데이터 모델 | dbdiagram ERD 한 장 + 주황 말풍선 "핵심 설계 포인트"(Enum 상태값, M:N 해소, 정규화 이유) | ERD PNG + 말풍선: M:N 4개, enum, 논리 설계 ↔ 시연 앱 저장 매핑, [설계] 로그인 |
| API | Swagger 화면 + YAML 일부 + "핵심 설계 포인트"(공통 스키마, 에러 코드) / Appendix 에 화면별 API 1:1 매핑·스키마 목록 | Swagger 목록 캡처 + 포인트(`$ref` 스키마 34, 오류 401·404·409·422·503, WebSocket 은 description) + 화면↔API 매핑 표 |
| 규모 | 액터 3, 화면 약 18, API 25, 테이블 8, 관계 10, 에러 400·401·403·404·409 | WME: 액터 2+외부 2, 화면 13, API 16, 테이블 19, 관계 23 — **화면·API 규모는 비슷, 테이블만 많다** (결정 17: 유지, 그룹 설명) |
| 기타 | "최소 10개 이상 Page 필요", securitySchemes 없음(인증은 description 에 글로) | 화면 13개 ✅, WME 도 securitySchemes 없음 ✅ |

## 6. 코드 대조에서 찾은 UI ↔ API 불일치 (2026-09-14)

| # | 내용 | 조치 |
|---|---|---|
| M1 | `PATCH /api/enrollments/{id}/progress` 를 **어떤 화면도 부르지 않는다** → 진도(`stepsCompleted`)가 시드에서 안 바뀐다 | FRONT — 실습 단계 완료 시 호출 |
| M2 | `POST /api/attempts/{id}/feedback/viewed` 를 **프론트가 부르지 않는다** | FRONT — 결과 화면에서 피드백이 보이면 호출 |
| M3 | 로그인 화면(아이디·비밀번호 입력)에 대응 API 없음 | R5 로 설계에 추가 |
