# 진행 상황

> 이 파일은 **완료 · 진행 중 · 다음 작업**만 담는다. 결정의 배경은 `../PROJECT_HEAD.md`,
> 전체 계획은 `SEMI_ON_PROJECT_PLAN.md` 를 본다.
> 세션별 담당은 `기획/프롬프트/README.md`, **이어받기는 루트의 `이어서작업.md`** 를 본다.
> 최종 수정: 2026-09-14

## 한 줄 상태

프론트·백엔드 연결 완료. 그 위에 **범용성 구조(채점 데이터화 · `exercise_type` · 조작 없는
judgment 실습)** 와 **3D 장비 뷰(노광·현상 연출 포함)** 까지 올라갔다.
남은 것은 **센서 실측 · LLM 실제 호출 · 제출 문서**다.

## 완료

### 기획·결정
- [x] 서비스명 **WME (We Make Experts)** 확정
- [x] 주제·방향 확정 — B2B 사내 기술교육 플랫폼, 실습 과정 1개 완전 구현
- [x] 전체 계획서 작성 (18절)
- [x] 실습 과정을 **마스크·웨이퍼 정렬 실습**으로 전환, `설계/실습과정_정렬.md` 작성
- [x] 포지셔닝 확정 — "실장비 대체"가 아니라 "투입 전 판단 훈련"
- [x] AI 역할 정리 — LLM 피드백이 주, 규칙 기반 정렬 경로 분석이 부
- [x] 세션 4개 분담과 프롬프트 (`세션/`), 세션별 로그 분리 (`기획/작업로그/`)
- [x] 발표 5분 구성과 시연 안전장치 확정
- [x] **Front·Back 교차 확인** — 실제 코드 대조로 불일치 7건 발견, 세션별 배정
- [x] **결정 9건 확정** (2026-09-12) — 상세는 `이어서작업.md` 3.5단계
      과정 ID `photo-align` / 입력 장치명 `inputDevice`·`model_controller` /
      기울기 변환은 프론트 계산 / 계수는 과정 설정값 / 센서는 Web Serial 직통 /
      3축 매핑(roll→X, pitch→Y, yaw→θ, 속도 방식) / 서버 시드를 프론트에 맞춤 /
      `VITE_API_MODE` 안전장치 / 버전 `course-2.0.0` 통일

### 프론트엔드
- [x] React + Vite + TypeScript + Tailwind v4 + Recharts 구성
- [x] 사이드바 셸, 라이트 SaaS 디자인 토큰, 역할 전환(신입사원/매니저)
- [x] 업종 중립 구조 — 업종·고객사 의존 내용은 `src/data/tenant.ts` 와 `courses.ts` 에만
- [x] 타입(DB 6테이블과 1:1), `src/api` 접근 계층, `src/lib/stats` 집계
- [x] 시드 데이터 — 학습자 8명, 시도 12건. 화면 수치는 전부 이 기록에서 계산
- [x] 화면 11개 (`/login`, `/learn`, `/learn/status`, `/learn/courses`, `/learn/records`,
      `/courses/:id`, `/attempts/new`, `/attempts/:id/result`,
      `/instructor`, `/instructor/learners`, `/instructor/learners/:id`)
- [x] 역할별 라우트 가드, 그래프 6종(색 검증 통과)
- [x] **정렬 실습으로 전면 교체** — 정렬 마크 SVG, 실시간 오차 표시, 보정 궤적,
      조정 순서·이유 제출 폼, 규칙 기반 샘플 채점(`AI 미연결` 표시)
- [x] **입력 어댑터 `src/input/`** — `ControllerSource`(출력은 속도 `{vx,vy,vTheta}`)
      + `KeyboardSource`(사용 중) + `TiltSource`(센서 자리, `available:false`)
- [x] 타입 검사 · 빌드 · 정적 검사 통과

### 백엔드
- [x] `schema.sql` 6테이블 — `실습과정_정렬.md` 9절 반영, sqlite3 생성 확인
- [x] `analysis.py` **규칙 기반 정렬 경로 분석** (모델 학습 없음) —
      보정 구간, 과잉 보정, 축 간섭, 수렴 패턴. 합성 궤적 5종으로 검증
- [x] `main.py` REST 10개 + WebSocket 1개 — curl 로 동작 확인
- [x] `llm/` 입력 구성 · 출력 스키마 · 응답 검증 · 재시도 분리
- [x] 이상 탐지 시절 필드 전면 교체 — `orderOptionId` / `aligning`·`aligned` /
      phase `idle`·`aligning`·`confirmed`·`submitted` / `events.axis` 는 `metrics_json` 안
- [x] 과정 응답을 프론트 `Course` 타입에 맞춘 **평평한 구조**로 변환
- [x] `content_json` 에 `tolerance`(4px / 1.0°) + `control`(변환 계수) 저장
- [x] 시드 — 학습자 8명 + 담당자 1명, 배정 16건, 시도 12건, 과정 5개.
      루브릭이 낮을수록 과잉 보정이 많게 궤적 생성. 피드백은 전부 `generatedBy:"mock"`
- [x] 분석기 속도창 버그 수정 (200ms 창 번짐으로 보정 구간이 하나로 붙던 문제)

## 진행 중

- [ ] **프론트 ↔ 백엔드 연동** (FRONT) — 지시문은 `이어서작업.md` 4단계에 있다
      할 일 0 `VITE_API_MODE` 스위치 → 1 이름 통일 → 2 fetch 전환 → 3 실습 흐름 → 4~6
- [ ] **설계 문서 갱신** (HEADER) — 아래 "다음 작업" 참고

## 다음 작업

### 설계·제출 문서 (HEADER) ← 지금 우선순위 1
- [ ] `설계/DB설계.md` **전면 갱신** — 현재 내용이 이전 컨셉(`anomaly_score`,
      `anomaly_candidate`, `measuring/measured`)이라 실제 `schema.sql` 과 다르다.
      **제출물 「DB 다이어그램」이 여기서 나온다** (mermaid ERD → PNG 내보내기)
- [ ] `설계/API명세서.md` — 경로 목록만 있고 상세가 비어 있다. 서버가 확정됐으니 작성 가능. **제출물**
- [ ] `설계/화면설계.md` — 11화면 구조와 상태
- [ ] `설계/실습과정_정렬.md` 11절 갱신 — 허용 오차·조작 계수를 확정으로 올리고,
      회전(θ)은 "드리프트 측정 후 결정"으로 기록
- [ ] `PROJECT_HEAD.md` 갱신 — 결정 9건 반영, 5절 FE 작업 순서에 BE 진행 상황 반영
- [ ] `제출/프로젝트기술서.md` (8절) → 발표 대본 5분 → 발표자료

### 서버·AI (BACK)
- [ ] **LLM 실제 호출 1회 성공** — API 키 확보 후. 지금까지 가짜 응답으로만 검증했다
- [ ] 프론트 연동 중 서버 쪽 문제 대응

### 센서 (SENSOR) — 내일 부품 도착 예정
- [ ] USB 연결·배선 확인 (충전 전용 케이블이면 데이터가 안 간다)
- [ ] **드리프트 측정** — 1분 정지 상태에서 roll/pitch 흔들림과 yaw 밀림.
      허용 오차(±1.0°)보다 크면 θ는 화면 버튼으로 간다
- [ ] 최소 스케치 — 시간(ms), roll, pitch, yaw 시리얼 출력
- [ ] 측정 기록을 `data/raw/` 에 저장

## 막힌 것 · 확인 필요

- **LLM API 키 확보 경로** — 미확인. 서버는 키만 꽂으면 되는 상태다
- 보드 모델, MPU6050 사양, 납땜 필요 여부 — 내일 부품 도착 후 확인
- 수업 제공 템플릿 / 배포 요구 여부, 제출물 양식 지정 여부 — 미확인

## 하지 않은 것 (오해 방지)

- **프론트 ↔ 백엔드 연결: 미완.** 지금은 각자 따로 돈다
- **LLM 실제 호출: 0회.** 화면·서버의 피드백은 전부 규칙 기반 샘플(`generatedBy:"mock"`)이며
  화면에 그렇게 표시한다
- 센서 연결, 실제 측정: 미수행
- 제출용 PDF·발표 자료: 미작성
- 화면·DB의 학습 기록은 시연용 시드이며 실제 교육생 기록이 아니다
- 허용 오차(4px / 1.0°)는 **교육 과정 설정값**이며 실제 장비의 정렬 정밀도가 아니다
