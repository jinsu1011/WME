# 이 파일은 예시다. 실제 키를 여기에 적지 않는다.
# .env 는 .gitignore 에 있으므로 깃에 올라가지 않는다.
# 키는 서버를 띄우는 셸의 환경변수로만 넣는다:
#   export WME_LLM_API_KEY="...."

WME_LLM_API_KEY=
WME_LLM_MODEL=claude-sonnet-5
# WME_DB_PATH=../../data/local/wme.db
