# WME (We Make Experts) — 프로젝트 헤드 문서

> 이 파일이 프로젝트의 기준점이다. 세부 기획 근거는 `기획/SEMI_ON_PROJECT_PLAN.md`, 이어받기는 `이어서작업.md`, 세션별 규칙은 `기획/프롬프트/` 을 따른다.
> 최종 수정: 2026-09-14 (0절 현재 상태 갱신. 1~8절 결정·원칙은 그대로이며, 5절 작업 순서는 9/12 시점 기록이다)

> **다른 노트북에서 이어받거나 새 대화를 시작한다면 `이어서작업.md` 를 먼저 연다.**
> 역할별 붙여넣기용 프롬프트와 지금까지의 진행 상황이 거기에 있다.

## 0. 지금 상태 요약

> 2026-09-14 기준. 자세한 진행은 `기획/PROGRESS.md`.

- 프론트엔드: **동작.** 화면 11개 + 실습 2종(정렬 `photo-align` / 판단 `defect-report`), 3D 장비 뷰, mock·server 전환
- 백엔드: **동작.** SQLite 6테이블, REST 15개 + WS 1개, 규칙 기반 정렬 경로 분석·채점(채점 규칙은 과정 데이터)
- 센서: Arduino UNO + MPU6050 **수신 성공.** 드리프트·축 부호·되돌아오기는 **미측정**
- 실제 LLM 연동: 코드 완성, **실제 호출 0회.** 회사에서 OpenAI 키를 받기로 함
- 수업은 **9/15 시작**. 제출물 양식은 아직 받지 못함
- 작업은 **세션 4개**로 나눈다: HEADER / FRONT / BACK / SENSOR (5.2절).

> ⚠️ **실습 컨셉이 바뀌어 이 문서 일부가 옛 내용이다.** 3.1절 데이터 소스 어댑터(mock/replay/live)·
> 3.3절 이상 탐지(IsolationForest)·5.4절 발표 구성의 "이상 → 근거 선택" 은 이전 「이상 대응 실습」 기준이다.
> 지금 기준은 `기획/설계/실습과정_정렬.md` 와 `이어서작업.md` 3.5단계 결정표다.

## ⚠️ 실습 과정 변경 (2026-09-12)

대표 실습 과정이 **「포토공정 입문 — 마스크·웨이퍼 정렬 실습」** 으로 바뀌었다.
이전의 「웨이퍼 스테이지 이상 대응 실습」은 폐기한다.

**정의는 `기획/설계/실습과정_정렬.md` 가 기준이다.** 화면·DB·AI는 전부 그 문서를 따른다.

바뀐 것 요약:
- 센서는 이상 탐지 대상이 아니라 **화면 속 웨이퍼를 움직이는 교육용 컨트롤러**다
- IsolationForest 학습이 **불필요**해졌다 → 발표장 오탐 위험 소멸
- 센서가 없어도 키보드로 실습 전체가 동작한다
- 루브릭이 정렬 정확도 / 조정 순서 / 보정 효율 / 설명·기록 으로 교체된다

## 1. 제품 정의 (확정, 바꾸지 않음)

**장비를 운용하는 기업에 판매하는 B2B 사내 기술교육 플랫폼.**
제품 자체는 업종 중립이고, **이번 데모는 반도체 장비 기업에 납품한 인스턴스**라는 설정으로 만든다.

- 구매자: 기업 교육 담당자 / 기술교육센터
- 사용자: 신입·전환배치 기술자(교육생), 강사·교육 담당자
- 핵심 흐름: 과정 탐색 → 개념 학습 → 센서 실습 관찰 → 근거 기반 판단 → AI 피드백 → 재실습 → 교육 기록 조회
- 설비 관제 SaaS / 손동작 교정 / 게임으로 되돌리지 않는다. 이상 탐지는 **한 교육 과정 안의 실습 도구**다.

### 포지셔닝 (이 문장으로만 설명한다)

> **실장비 실습을 대체하는 것이 아니다. 실장비에 투입되기 전에,
> 신호를 읽고 무엇을 먼저 확인할지 판단하는 훈련을 저비용으로 반복하는 것이다.**

실장비 실습은 장비 점유·지도 인력·사고 위험이 모두 비용이다. 그래서 기업이 돈을 낸다.
다만 우리가 가르치는 것은 **정비 기술이 아니라 관찰과 판단**이다.

- ❌ "모형으로 실장비 실습을 대체한다" → "장난감으로 정비를 배우냐"는 반박에 무너진다
- ✅ "실장비 투입 전 판단 훈련" → 모형의 한계가 공격이 아니라 **의도된 설계**가 된다

모형·소품은 발표에서 반드시 **교육용 모형**이라고 명시한다. 숨기려다 들키면 잃는 게 훨씬 크다.

## 2. 확정한 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| FE 스택 | React + Vite + **TypeScript**, Tailwind CSS, Recharts, React Router | 계획서 스택과 일치. 타입이 있으면 BE 연동 시 데이터 구조 불일치를 컴파일 단계에서 잡는다 |
| 업종 중립성 | **코드는 업종 중립, 업종 내용은 전부 시드 데이터** (`src/data/tenant.ts`, `courses.ts`) + 화면 고객사 배지 | "다른 회사엔 데이터만 갈아끼우면 된다"를 말이 아니라 구조로 증명 |
| 데모 고객사 | SK 하이닉수 (가상 고객사로 표기) | 발표용 예시. 실제 기업 데이터가 아님을 화면에 명시 |
| 서비스명 | **WME (We Make Experts)** 로 확정 (2026-09-12). 이전 가칭 SEMI:ON | 화면·문서·발표에 같은 이름을 쓴다. 화면 표기는 `product` 상수 한 곳에서만 나온다 |
| 성취도 표현 | 과정 루브릭 4기준 달성도(막대) + 시도별 추이(선) + 주차별 활동(막대) + 학습자별 진도(막대) | 진도(얼마나 했나)와 달성도(얼마나 기준을 충족했나)를 분리해서 보여준다 |
| 디자인 톤 | 라이트 기업 SaaS (밝은 배경, 절제된 색, 카드 중심) | 발표 빔프로젝터 가독성 + 교육 서비스 성격 |
| 센서 공백 대응 | **데이터 소스 어댑터 3종**(mock / replay / live) | 센서가 와도 화면 코드를 다시 쓰지 않는다 |
| BE 시점 | FE가 형태를 갖춘 뒤 FastAPI 착수. 그 전까지 FE 내부 mock API 계층 | 3일 일정에서 FE가 평가 비중이 크다 |
| AI 구성 | **LLM 학습 피드백이 주(主), 센서 이상 탐지가 부(副)** | 이상 탐지만 있으면 교육 서비스가 아니라 장비 관제 도구가 된다 |
| 루브릭 채점 | LLM이 답변을 루브릭 4기준으로 채점 → 그 값이 성취도 그래프가 된다 | "AI가 어디 쓰였나"에 대한 답이 화면으로 보인다 |
| 세션 분담 | HEADER / FRONT / BACK / SENSOR 4개, 폴더 소유권으로 분리 | 대화끼리 기억을 공유하지 않으므로 담당 폴더를 겹치지 않게 한다 |

## 3. 센서 없이 작업하기 위한 핵심 설계

### 3.1 데이터 소스 어댑터

화면은 데이터가 어디서 오는지 모른다. 소스만 갈아끼운다.

```
SensorSource (인터페이스)
├── MockSource   : 합성 파형 생성 (지금 사용, 발표 백업으로도 유지)
├── ReplaySource : 저장된 측정 JSON 재생 (센서 확보 후 녹화본)
└── LiveSource   : WebSocket 실시간 수신 (센서 + BE 완성 후)
```

- 화면 상단에 **출처 배지**를 항상 표시한다: `예시 데이터` / `저장된 측정 재생` / `실시간 모형 측정`.
- 합성 데이터를 실측인 것처럼 보이게 하지 않는다. 이건 발표에서 감점이 아니라 신뢰 요소다.

### 3.2 API 계층 분리

`src/api/*.ts` 함수 시그니처를 계획서 REST 명세와 1:1로 맞추고, 지금은 내부 mock 구현 + localStorage 영속으로 동작시킨다.
BE 완성 시 함수 본문만 `fetch`로 교체하면 화면은 수정 없음.

### 3.3 AI 두 개의 역할과 순서

| | AI 1 — 이상 후보 탐지 | AI 2 — 학습 피드백 (LLM) |
|---|---|---|
| 역할 | 정상과 다른 구간을 **근거로 제시** | 학습자의 판단에 **피드백하고 루브릭 채점** |
| 센서 | 필요 | **불필요** |
| API 키 | 불필요 (로컬 scikit-learn) | 필요 |
| 담당 | SENSOR 세션 | BACK 세션 |
| 중요도 | 부 | **주** |

**AI 2가 이 제품의 핵심이다.** 이상 탐지만 있으면 장비 관제 도구이고, 그런 건 이미 많다.
학습자가 낸 판단에 피드백이 돌아가야 교육 서비스다.

AI 2가 센서 없이 가능한 이유: LLM에 넣는 입력이 센서 원본이 아니라
**계산된 관측 결과 + 학습자 답변 + 루브릭**이기 때문이다. mock 데이터에서도 같은 숫자가 나온다.
센서가 오면 숫자의 출처만 바뀌고 AI 쪽은 손댈 게 없다.

지금은 규칙 기반 **샘플 피드백**에 `AI 미연결 — 샘플 피드백` 라벨이 붙어 있다.
실제 LLM 연결 시 라벨을 제거하고 `feedback_status`를 실제 값으로 바꾼다.

**판단은 학습자가 한다.** AI가 정답을 내려주는 화면으로 만들면 교육 서비스가 아니라 자동 진단기가 된다.

## 4. 폴더 구조

```
미니프로젝트1/
├── 이어서작업.md                # 다른 노트북·새 대화에서 이어받을 때 여는 파일
├── PROJECT_HEAD.md              # 제품 정의·결정·원칙 (이 파일)
│
├── 기획/
│   ├── SEMI_ON_PROJECT_PLAN.md  # 전체 계획서
│   ├── PROGRESS.md              # 완료·진행 중·다음 작업
│   ├── 프롬프트/                # 이어서 할 프롬프트 (HEADER/FRONT/BACK/SENSOR)
│   ├── 작업로그/                # 역할별 작업 로그
│   ├── 설계/                    # 실습과정 · DB설계 · API명세서 · 화면설계 (제출물 원본)
│   └── 이전기록/                # 최초 기획서 원본
│
├── web/
│   ├── frontend/                # React 화면 11개
│   ├── backend/                 # FastAPI · SQLite · LLM 피드백
│   └── README.md                # 설치·실행 방법
│
├── hardware/
│   ├── firmware/                # Arduino 스케치 (wme_sensor)
│   └── README.md                # 부품·배선·센서 부착 설명
│
├── data/
│   ├── raw/                     # 센서 측정 기록 (드리프트 측정 등)
│   ├── demo/                    # 발표용 재생 데이터
│   └── local/                   # SQLite DB (wme.db)
│
└── 제출/                        # 제출물 + 체크리스트.md
```

실행:

```bash
npm --prefix web/frontend run dev
```

## 5. FE 작업 순서 (센서 없이 전부 가능)

- [x] **F0** 프로젝트 스캐폴드, 디자인 토큰, 레이아웃 셸(헤더 / 고객사 배지 / 데모 역할 전환) — 2026-09-12 완료, `npm --prefix web run dev`로 확인
- [ ] **F1** 타입 + 시드 데이터 + mock API 계층 (5과정 카탈로그, 교육생 1·담당자 1)
- [x] **F1** 타입 + 시드 데이터(교육생 8명·시도 15건) + `src/api` 접근 계층 + `src/lib/stats` 집계
- [x] **F2** 학습 홈 `/learn` — 지표 4종, 기준별 달성도, 시도별 추이, 주차별 활동, 내 과정 진도
- [x] **F3** 과정 상세 `/courses/:id` — 목표·단계·평가기준·준비물, 미리보기/준비 중 분기
- [~] **F4** 실습 `/attempts/new` — 단계 스테퍼·장치 상태·장치 개념도·답변 폼 배치까지. 차트와 측정은 센서 확보 후
- [x] **F5** 결과 `/attempts/:id/result` — 제출 답변, 관측 요약, 기준별 확인, 샘플 피드백, 시도 목록, 버전 기록
- [x] **F6** 담당자 `/instructor`, `/instructor/learners`, `/instructor/learners/:id` — 반 요약·학습자별 진도·개인 성취도 드릴다운
- [~] **F7** 예외 상태 — 준비 중/미배정/미제출/센서 미연결/기록 없음 구현. 로딩·AI 실패는 BE 연결 후
- [ ] **F8** 발표 해상도 점검, 가독성 정리

### 화면 목록 (구현됨)

| 경로 | 역할 | 내용 |
|---|---|---|
| `/login` | 공통 | 데모 로그인 (신입사원 `1`/`1`, 매니저 `2`/`2`). 인증 구현이 아님을 화면에 표시 |
| `/learn` | 신입사원 | 실습 목록 — 해야 할 실습(위) / 완료한 실습(아래) |
| `/learn/status` | 신입사원 | 현재 상황 — 연습 횟수·시간·진도·정확도 변화 |
| `/learn/courses` | 신입사원 | 과정 카탈로그 |
| `/learn/records` | 신입사원 | 실습 기록 목록 |
| `/courses/:id` | 신입사원 | 대기방 — 진행 방법·확인 기준·준비물 + `연습 시작` |
| `/attempts/new` | 신입사원 | 연습(센서 미연결 상태 표시) |
| `/attempts/:id/result` | 공통 | 학습 결과 + 피드백 |
| `/instructor` | 매니저 | 반 전체 현황 |
| `/instructor/learners` | 매니저 | 학습자 목록 |
| `/instructor/learners/:id` | 매니저 | 개인별 성취도 |

센서 확보 후: 보드 배선 확인 → 정상 세션 녹화 → ReplaySource → FastAPI + WebSocket → IsolationForest 또는 통계 기준 → 실제 LLM.

## 5.1 그래프 규칙

- 계열 색은 검증된 팔레트에서 가져오고(blue `#2a78d6`, orange `#eb6834`, 회색 기준선 `#8493a8`),
  색맹 시뮬레이션·대비 검사를 통과한 조합만 쓴다.
- 색 하나로 뜻을 싣지 않는다. 범례 + 값 라벨 + 상태 텍스트를 항상 함께 둔다.
- 축은 하나만 쓴다. 서로 다른 단위를 한 차트에 겹치지 않는다.
- 모든 수치는 저장된 기록에서 계산한다. 그래프에 숫자를 손으로 적어 넣지 않는다.

## 5.2 세션(AI) 역할 분담

여러 대화를 쓰되 **대화끼리는 기억을 공유하지 않는다.** 그래서 담당 폴더를 겹치지 않게 나눈다.
상세 규칙과 프롬프트는 `기획/프롬프트/` 에 있다.

| 세션 | 담당 | 쓰기 가능 폴더 |
|---|---|---|
| **HEADER** | 기획·문서·의사결정·발표 | `PROJECT_HEAD.md`, `기획/`, `제출/` |
| **FRONT** | 화면 | `web/frontend/` |
| **BACK** | API·DB·**LLM 피드백** | `web/backend/`, `data/local/` |
| **SENSOR** | 펌웨어·데이터 수집·**이상 탐지 모델** | `hardware/`, `data/` |

규칙 세 가지:

1. **남의 폴더는 읽기만 한다.** 고쳐야 하면 담당 세션에서 한다.
2. **결정은 HEADER에서만.** 이름·범위·구조 변경이 다른 세션에서 나오면 거기서 정하지 말고 HEADER로 가져온다.
   세션마다 다른 결정을 내리는 게 이 방식의 유일한 실패 원인이다.
3. **시작할 때 `PROJECT_HEAD.md` + `기획/PROGRESS.md` 읽고, 끝날 때 `PROGRESS.md` 갱신.**

LLM 피드백은 BACK 담당이다. API 키를 들고 호출하는 쪽이 서버이므로, 따로 떼면 같은 파일을
두 세션이 건드린다. SENSOR의 AI는 이상 탐지 모델만이다.

## 5.3 노트북 두 대로 작업할 때

- **프롬프트·문서에 절대 경로를 적지 않는다.** 노트북마다 경로가 다르다. 프로젝트 폴더를 열고 시작한다.
- **`node_modules` 는 동기화하지 않는다.** 노트북마다 `npm install` 을 따로 한다.
- 노트북을 바꾸기 전에 **반드시 `기획/PROGRESS.md` 와 `이어서작업.md` 를 갱신한다.**
  대화 기록은 옮겨지지 않는다. 옮겨지는 건 파일뿐이고, 그게 유일한 인수인계 수단이다.
- 같은 파일을 두 노트북에서 동시에 열지 않는다. 클라우드 동기화는 충돌 사본을 만든다.

## 5.4 발표 시연 안전장치

실시간 센서 시연은 **가장 깨지기 쉬운 부분**이다. 손으로 움직이는 동작은 재현되지 않고,
집 책상에서 잡은 정상 기준이 발표장 테이블에서는 이상으로 뜰 수 있다.

1. **발표장에서 기준을 다시 잡는다.** 리허설 때 그 테이블에서 정상 세션을 새로 녹화해 기준 교체.
2. **저장된 재생 데이터를 항상 대기시킨다.** 화면에 `저장된 측정 재생` 배지가 뜬다.
3. **시연 영상 백업**을 준비한다.
4. 모델이 불안정하면 **통계 기준 비교로 내린다.** 그렇게 말하는 건 감점이 아니다.
   손으로 정한 임계값을 "학습된 AI"라고 부르는 게 감점이다.

### 발표 5분 구성

| 시간 | 내용 |
|---|---|
| 0:00 | 센서 흔들기 — "이 신호를 본 신입은 뭘 먼저 확인해야 할까요?" |
| 0:20 | 실장비 실습은 비싸다 → 투입 전 판단 훈련을 반복하는 B2B 사내교육 |
| 0:45 | 과정 카탈로그 (업종만 바꾸면 다른 회사에도) |
| 1:15 | **★ 실습: 정상 관찰 → 이상 → 근거 선택 → 답변 → AI 피드백** |
| 3:00 | FE·센서·AI·DB 역할과 실제 구현 범위 |
| 3:45 | **★ 담당자 화면 — 방금 그 기록이 여기 떠 있다** |
| 4:30 | 검증한 범위와 모형의 한계 |

★ 두 개가 핵심이다. **센서 흔들기는 후크지 본편이 아니다.**
담당자 화면이 "그래서 회사가 왜 사냐"에 대한 답이다. 교육생 화면만 보여주면 개인용 학습 앱이 된다.

## 6. 지키는 원칙

1. 합성/재생/실측 데이터를 항상 구분 표시한다.
2. 이상도는 상대적 근거이지 고장 확률이 아니다. 원인 단정 문구를 쓰지 않는다.
3. 재실습은 새 시도로 저장하고 이전 기록을 지우지 않는다.
4. 강사 화면 숫자는 실제 저장된 데모 기록에서만 나온다. 가짜 성과 수치 금지.
5. 로그인·역할 구분은 데모 탐색 기능이며 인증 구현이 아니라고 화면에 표시한다.
6. 구현한 것과 계획한 것을 문서·발표에서 분리해 말한다.
7. **판단은 학습자가 한다.** AI는 근거를 제시하고 피드백할 뿐, 정답을 확정하지 않는다.
8. **"실장비 대체"라고 말하지 않는다.** 투입 전 판단 훈련이다.
9. 모형·소품은 교육용 모형이라고 명시한다.

## 7. 과제 공지에서 확인된 것 (2026-09-12 수신)

- 주제는 자유다. B2B/B2B2C/B2C 무엇이든 가능 → 현재 B2B 방향 유지에 문제 없음.
- **Front-End 중심의 서비스 기획**이 평가의 중심. BE는 "화면에 필요한 데이터를 저장하는 목적"의 DB 설계까지.
- 제출물: **프로젝트 기술서 PDF, API 명세서, DB 다이어그램** (3일차 13:00~14:00).
- 발표: 반별 개인 발표 5분, 퀴즈 없음. 이름·네이밍의 창의성을 명시적으로 언급함 → 서비스명 결정 필요.
- 배포(호스팅) 요구는 공지에 없다. 로컬 데모 전제를 유지하되 1일차에 확인한다.

이 공지대로면 **센서·실시간 수집은 가점 요소이지 필수 요건이 아니다.** 센서를 못 구해도
과제 요구는 화면·기획·DB·API로 충족된다. 우선순위를 그렇게 잡는다.

## 8. 열린 항목

- [x] **서비스명 확정** — WME (We Make Experts), 2026-09-12
- [ ] **LLM API 키** — 1일차에 수업에서 지급하는지 먼저 확인. 없으면 개인 키 필요
- [ ] 수업 제공 템플릿/배포 요구 여부 (Day 1 오리엔테이션에서 확인)
- [ ] 보드 모델, MPU6050 보유 여부, 핀헤더 납땜 필요 여부
- [ ] 사용할 LLM API와 키 확보 경로
- [ ] 제출물 양식 지정 여부
