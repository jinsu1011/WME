# 이어서 작업하기 — WME

> **다른 노트북에서, 또는 새 대화에서 작업을 이어받을 때 이 파일부터 연다.**
> 대화 기록은 노트북 사이로 옮겨지지 않는다. 옮겨지는 건 이 폴더의 파일뿐이다.
> 그래서 **작업을 끝낼 때마다 이 파일을 갱신**해야 다음 사람(=다음 대화)이 이어받을 수 있다.
>
> 마지막 갱신: **2026-09-15 새벽 (집 맥북, 센서 완료 후 마감) / HEADER**
> 제출자: **9반 김진수**

---

## 1단계 — 노트북에 앉으면 (집·회사 공통)

```bash
git pull          # 다른 노트북에서 한 작업 받기
./시작.sh         # 이 노트북의 설치물·DB 확인, 깨졌으면 그 자리에서 재설치
```

`준비 완료` 가 나오면 끝이다. 빨간 줄이 나오면 그 문장대로 한다.

`./시작.sh` 가 하는 일 (몇 번 실행해도 된다, 멀쩡하면 아무것도 안 바꾼다):
- `web/frontend/node_modules` → `deps.nosync/node_modules` 링크가 멀쩡한지, `package-lock.json` 이 바뀌었는지 → 아니면 `npm ci` 재설치
- `web/backend/.venv` → `.venv.nosync` 링크가 멀쩡한지, `requirements.txt` 가 바뀌었는지 → 아니면 venv 재생성
- iCloud 가 만든 **빈** 충돌 사본(`node_modules 2`, `.venv 2`) 삭제. 비어 있지 않으면 멈추고 알린다
- DB(`data/local/wme.db`)가 없으면 시드로 만든다

> 왜 이렇게 하나: 이 폴더는 **iCloud 로 두 노트북이 같이 쓴다.** 설치물은 노트북마다 달라야 해서
> 동기화에서 뺐다(`.nosync`). 따라온 설치물을 고쳐 쓰면 다른 노트북 경로를 가리켜 실행되지 않는다(9/14 겪음).
> 집 맥북: 2026-09-15 `./시작.sh` 정상 확인. **회사 맥북: 아직 실행해 본 적 없음 — 처음 한 번은 설치에 1~2분.**

### 실행 — 창 두 개

```bash
cd web/backend && ./.venv/bin/python -m uvicorn app.main:app --port 8000
```

```bash
npm --prefix web/frontend run dev:server     # http://localhost:5174 (서버 데이터)
```

- `npm --prefix web/frontend run dev` 는 **mock 모드(5173)** — 서버 없이 시드로 돈다. 발표장 백업·센서 확인용
- **`--reload` 를 쓰지 않는다** (iCloud 폴더에서 2분마다 죽는다)
- 포트가 "이미 사용 중"이면 다른 창에서 떠 있는 것이다. 그걸 쓴다
- **데모 로그인**: 신입사원 `1`/`1` (김진수), 매니저 `2`/`2` (최태원) — 인증 구현이 아니라 데모 전환
- **센서는 크롬 + localhost 에서만** (Web Serial). 아두이노 IDE 시리얼 모니터는 닫는다
- DB 를 처음 상태로: `cd web/backend && ./.venv/bin/python -m app.seed` (⚠️ 시도 기록이 전부 지워진다)

### LLM 키 (받으면)

`web/backend/.env` 의 `WME_LLM_API_KEY=""` **따옴표 안에 키만.** 이 파일은 깃에 올라가지 않는다(노트북마다 넣는다).

```bash
cd web/backend && source .env && ./.venv/bin/python -m uvicorn app.main:app --port 8000
```

`http://localhost:8000/api/health` 의 `llmConfigured` 가 `true` 면 들어간 것이다.

### 새 컴퓨터에서 처음이면

```bash
git clone https://github.com/jinsu1011/WME.git    # 비공개 저장소, GitHub 로그인 필요
cd WME && ./시작.sh
```

---

## 2단계 — 떠나기 전에 (집·회사 공통) ★

```bash
./정리.sh                         # 8000·5173·5174 서버 종료 (DB 가 iCloud 로 넘어가므로 켠 채 떠나지 않는다)
git add -A
git commit -m "무엇을 했는지 한 줄"
git push
```

- **두 노트북에서 동시에 작업하지 않는다**
- 각 세션은 끝날 때 `기획/작업로그/<세션>.md` 맨 위에 기록 → HEADER 가 모아 `기획/PROGRESS.md` 와 이 파일 갱신

---

## 3단계 — 지금 어디까지 왔나 (2026-09-15 새벽)

### 한 줄 요약

**구현은 끝났다(센서 포함).** 남은 건 **제출물 완성** — BACK·FRONT 검증과 캡처 → HEADER 가 개요 PDF 조립 → 발표.

### 과제 핵심 — `기획/과제요건.md`, `제출/체크리스트.md`

- **기획·설계를 평가한다. 구현은 점수가 아니다** (3D·센서·LLM 은 캡처·설계 근거)
- 제출물 3개: `9반_김진수_WME-개요.pdf`(**UI 흐름도 캡처 필수**) · `9반_김진수_WME-API.yml` · `9반_김진수_WME-DB.dbml`
  ("판교캠퍼스" 포함 여부는 수업에서 확인). **발표자료는 제출물이 아니다**
- 평가: 납기 20 / 목표 시스템 정의 30 / 데이터 모델 20 / API 명세 20 / 발표 10
- 감점: **UI·API·ERD 불일치**, UI 필요 API 누락, 과도한 범위
- HEADER 자체 평가(9/14 밤): 지금 그대로 내면 약 4.5/10, 설계 내용만 보면 7.5/10. 캡처·PDF·M1·M2 가 끝나면 9+ 목표

### 일정

```
9/15 1일차   가이드: 서비스 개요·요구사항·UI 흐름도 / LLM 키 수령   ← 오늘 (회사)
9/16 2일차   가이드: 데이터 모델·API 명세
9/17 3일차   보완 → 13:30 제출 마감(미준수 0점) → 14:30 개인 발표 5분
```

### ▶ 다음 할 일 — 순서대로 (회사)

| 순서 | 누가 | 할 일 | 프롬프트 |
|---|---|---|---|
| 0 | 사용자 → **HEADER** | **HEADER 프롬프트를 가장 먼저** 새 대화에 붙여넣기 → HEADER 가 `git pull` → `./시작.sh` 실행 → "BACK·FRONT 붙여넣으세요" 알림 | `기획/프롬프트/HEADER.md` |
| 1 | 사용자 | 수업에서 확인: 파일명 "판교캠퍼스" 포함 여부, 발표 때 무엇을 띄우는지, LLM 키 | — |
| 2 | **BACK** | README `--reload` 삭제 → **API.yml 실서버 대조**(POST·PATCH·오류·WS) → **DB.dbml 매핑 대조** → 키 받으면 LLM 1회 → **테스트 기록 시드 재적재로 정리** | `기획/프롬프트/BACK.md` |
| 2 | **FRONT** (BACK 과 동시) | **M1 진도 기록·M2 피드백 열람 연결** → (BACK 시드 재적재 뒤) **캡처 24장** → `제출/캡처/` | `기획/프롬프트/FRONT.md` |
| 2 | **HEADER** (동시) | Swagger Editor·dbdiagram.io 에서 열어 확인 → BACK·FRONT 결과로 YAML·DBML·화면설계 보정 → 개요 PDF 조립 → 발표 5분 | `기획/프롬프트/HEADER.md` |
| — | SENSOR | 할 일 없음(완료). 발표 전 리허설 때만 보드 연결 확인 | — |

> 서버 켜고 끄기·시드 재적재는 **BACK 세션 하나만** 한다. FRONT 는 떠 있는 서버를 쓴다.

### 구현된 것 (실제로 돌려서 확인한 것만)

**프론트** `web/frontend`
- React + Vite + TypeScript + Tailwind v4 + Recharts + React Router + Three.js. 화면 11개(라우트) + 실습 2종
- mock(5173) / server(5174) 전환 — mock 은 발표 백업이라 지우지 않는다
- 3D 장비 뷰 + 정렬 확정 → 노광 → 현상 → 오차만큼 어긋난 패턴
- 키보드: 방향키 X/Y · Q/E 회전 · W/A/S/D 기울기(기울어지면 회전 잠김) · Shift 미세조정 (`event.code`, 한글 입력에서도 동작)
- **센서 모드 (9/14 밤, 실물 확인 "의도대로 됨")**
  - 값 다듬기(기울기 0.25초·비틀기 0.12초), 앞뒤 기울기 부호 반전(`SENSOR_AXIS_SIGN`)
  - 위치 X/Y 가운데 고정(`고정` 표시), **비튼 각도만큼 θ 가 그대로**, 수평 잠금 없음, ±0.5°·Q/E 미세조정
  - "port already open" 수정 — 화면을 떠나면 포트를 닫고, 새로 연결할 때 이전 연결을 먼저 닫는다
- **실습 성공 표시 (키보드·센서 공통)**: 정렬 시작 뒤 수평(±4°) + 허용 오차(±4px/±1°) 5초 유지 → 진행 막대 → `실습 성공`. **저장·채점과 무관한 화면 표시**
- **미완**: M1 진도 기록·M2 피드백 열람 API 를 화면이 부르지 않는다 (명세 불일치 → 감점 요인)

**백엔드** `web/backend`
- `schema.sql` 6테이블 + `courses.exercise_type`. REST 15 + WebSocket 1. 분석기 alignment(규칙 기반)·judgment, 채점 규칙은 과정 데이터
- `llm/` 입력·출력 스키마·검증·재시도. **실제 호출 0회**
- DB 시드: 학습자 8 + 매니저 1, 배정 17, 과정 5(실습 가능 2), 시도 14

**센서** `hardware/`
- Arduino UNO + MPU6050, 50Hz `WME,` 출력. 9/14 밤 펌웨어 수정(커밋 c1cb566): **보정 중 움직이면 멈추지 않고 재보정**(`ERR,KEEP_STILL_RETRY`), **타이밍 밀리면 재동기**(`INFO,TIMING_RESYNC`)
- 크롬에서 실물 모형으로 기울기·회전이 화면에 따라오는 것 사용자 확인
- ⚠️ SENSOR 작업로그(`기획/작업로그/Sensor.md`)는 9/13 이후 갱신되지 않았다. 업로드 결과 숫자 기록 없음
- 드리프트 3분 등 정식 측정은 하지 않았다 (결정 16 으로 필요 없어짐)

**제출물**
- `기획/설계/서비스개요.md` ✅, `기획/설계/화면설계.md` ✅(9/15 센서·실습 성공 반영, 캡처 목록 24장)
- `제출/WME-API.yml` 🟡 초안(GET 12종 대조 완료, POST·PATCH·오류 미대조), `제출/WME-DB.dbml` 🟡 초안(19테이블)
- **개요 PDF ❌, 캡처 0장 ❌, 발표 대본 ❌**

---

## 3.5단계 — HEADER 확정 결정 (2026-09-12 ~ 15)

새 대화에서 이걸 다시 논의하지 않는다. 이미 정해진 것이다.

| # | 결정 | 이유 |
|---|---|---|
| 1 | 과정 ID는 **`photo-align`** | 프론트·서버 양쪽 일치 확인 완료 |
| 2 | 입력 장치 필드는 **`inputDevice` / `keyboard`·`model_controller`** (서버 이름 기준) | DB 컬럼명이 제출물에 박히므로 서버를 기준으로 잡는다 |
| 3 | **기울기 → 이동 변환은 프론트가 계산**한다 | 조작은 즉시 반응해야 한다. 서버 왕복하면 조작감이 무너진다 |
| 4 | 변환 계수·허용 오차는 **과정 설정값**(`courses.content_json`)으로 둔다 | 코드에 박은 상수가 아니라 과정 설정이어야 설명이 된다 (센서 화면 보정값은 결정 16 예외) |
| 5 | 센서 데이터 경로는 **Web Serial API** (아두이노 → 브라우저 직통) | 서버를 거치지 않아 단순하다. 발표는 크롬으로 한다 |
| 6 | ~~모형 3축 매핑: 전부 속도 방식~~ → **결정 15·16 으로 대체** | 센서 모드는 X/Y 고정, 기울기 = 수평 판정, 비틀기 = 각도 그대로 |
| 7 | 서버 시드를 **프론트 시드에 맞춘다** (8명/12건) | 담당자 화면이 학습자 1명이면 제품 가치가 안 보인다 |
| 8 | 프론트에 **`VITE_API_MODE=mock\|server`** 스위치를 둔다 | 발표장에서 서버가 안 뜨면 mock 으로 되돌아간다 |
| 9 | 버전 문자열은 **`course-2.0.0`** 으로 통일 | 시도마다 복사 저장되는 값이라 두 개가 돌아다니면 추적이 무의미해진다 |
| 10 | **실습 화면 중앙에 3D 장비 뷰**(Three.js). 현미경 뷰(2D 마크)는 옆으로 | 실제 얼라이너가 스테이지+현미경 구성이다 |
| 11 | 범용성: 채점을 과정 데이터로, `exercise_type` 도입, 조작 없는 `judgment` 실습 추가 | "데이터만 갈아끼우면 된다"를 말이 아니라 증명으로 |
| 12 | 키보드 기울기 키 안내는 **`alignment.tiltControls`** (9/14) | 키보드 모드에서만 따로 보여 준다 |
| 13 | **설치물(`node_modules`·`.venv`)은 `.nosync` 폴더 + 링크, 노트북마다 새로** (9/14) → 9/15 부터 **`./시작.sh`·`./정리.sh`** 로 자동화 | iCloud 가 링크를 지우고 충돌 사본을 만든다 |
| 14 | **과제 요건 대응 R1~R9** (9/14 밤) — 제출물 DBML·OpenAPI YAML·개요 PDF / 3D·센서 유지(선택 입력 장치) / 센서 예외 흐름을 UI 흐름도에 / WebSocket·Web Serial 은 YAML 설명·고려사항에 / **로그인 API·비밀번호 해시는 `[설계]`로만** / **제출용 ERD 는 논리 설계로 정규화** / 액터 2종+외부 2종 / 에러 코드는 실제로 나는 것만 / 새 기능 금지 | 상세는 `기획/과제요건.md` 5절 |
| 15 | **센서 회전은 "비튼 각도 그대로"** (9/14 밤). 펌웨어는 보정 실패·타이밍 밀림 때 멈추지 않고 재시도 | 사용자 목표 "모형을 움직이는 대로 화면이 따라 움직임". 비틀기는 내려놔도 값이 유지돼 결정 6 의 우려에 해당하지 않는다 |
| 16 | **센서 모드 확정 (9/15, FRONT 보고 A~D)** — A: 센서 모드 수평 잠금 없음, 센서 모드에서 `control.yaw*` 속도 계수는 쓰지 않음(서버 값은 지우지 않음) / B: 센서 모드는 **평행·회전 연습용 선택 입력**. 위치 고정이라 '정렬 정확도'가 위치로는 자동 충족됨을 인정하고 **채점 코드는 안 바꾼다.** 기록은 `input_device` 로 구분, 캡처·발표의 결과 화면은 **키보드 모드 기록** 기준 / C: 회전 허용 **±1° 유지** (채점 범위와 묶여 있어 바꾸면 시드 점수가 달라진다) / D: 수평 ±4°·여유 1°·유지 5초·축 부호·다듬기 시간은 **화면 상수로 유지**(모형마다 다른 보정값). 서버로 올리지 않고 서비스개요 고려사항 10 에 기록. **실습 성공 5초 표시는 화면 표시이며 채점과 무관** | 새 기능 금지·마감 우선. 점수는 문서에서 나온다 |

**확정값**
- 허용 오차: 위치 **±4px**, 회전 **±1.0°** — *교육 과정 설정값이며 실제 장비의 정렬 정밀도가 아니다*
- 수평 판정 **±4.0°**(나갈 때 +1.0° 여유), 실습 성공 유지 **5초** — 화면 상수 `data/controllerSettings.ts`
- 키보드 계수 `movePxPerSec 90.0`, `rotateDegPerSec 22.0`, `fineFactor 0.25`
- 버전: `courseVersion course-2.0.0` / `modelVersion rule-align-1.0.0` / `settingsVersion align-settings-1.0.0`

**열린 결정**: 없음 (LLM 성공 시 화면·문서의 `AI 미연결` 표현 정리만 남음)

---

## 4단계 — 역할 프롬프트를 새 대화에 붙여넣기

**프롬프트는 `기획/프롬프트/` 폴더에 있다.** 파일 하나를 통째로 복사해서 새 대화의 첫 메시지에 붙여넣는다.

| 파일 | 언제 쓰나 | 건드리는 폴더 |
|---|---|---|
| `기획/프롬프트/HEADER.md` | 문서·제출물·발표, 결정 | `PROJECT_HEAD.md`, `이어서작업.md`, `기획/`, `제출/` |
| `기획/프롬프트/FRONT.md` | 화면 (M1·M2·캡처) | `web/frontend/`, `제출/캡처/` |
| `기획/프롬프트/BACK.md` | 서버·DB·LLM, 제출물 대조 | `web/backend/`, `data/local/` |
| `기획/프롬프트/SENSOR.md` | 아두이노 (9/14 완료) | `hardware/`, `data/` |
| `기획/프롬프트/FRONT_센서.md` | 센서 연결 화면 (9/14 완료) | `web/frontend/` |

규칙 세 개:
1. **폴더 소유권** — 남의 폴더는 읽기만 한다
2. **결정은 HEADER에서만**
3. **설치·서버 켜고 끄기·시드는 한 세션만**

---

## 지금 막혀 있는 것

- **LLM API 키** — 9/15 회사에서 받기로 했다. 못 받으면 발표에서:
  "LLM 피드백은 입력 구성·출력 스키마·응답 검증·재시도까지 구현했고 API 키 연결만 남았습니다.
  현재 화면의 피드백은 규칙 기반 샘플이며 화면에 그렇게 표시하고 있습니다."
- 파일명 "판교캠퍼스" 포함 여부 — 수업에서 확인

---

## 작업 로그

새로 추가할 때 **맨 위에** 적는다. 세션별 자세한 기록은 `기획/작업로그/` 에 있다.

| 날짜 | 노트북 | 역할 | 한 일 |
|---|---|---|---|
| 2026-09-15 | 집 맥북 | HEADER | FRONT 센서 보고 A~D 결정(결정 16), 화면설계·서비스개요에 센서 모드·실습 성공 반영(캡처 24장), 이어받기를 `시작.sh`/`정리.sh` 기준으로, 충돌 사본 정리, 회사용 HEADER 프롬프트 |
| 2026-09-14 밤 | 집 맥북 | FRONT·SENSOR | 센서 모드(비튼 각도 그대로·X/Y 고정·다듬기·부호), 실습 성공 5초 유지, port already open 수정, 펌웨어 재보정·재동기, `시작.sh`/`정리.sh` (c1cb566) |
| 2026-09-14 밤 | 회사 컴퓨터 | HEADER | 과제 PDF 수령 → 과제요건·서비스개요·화면설계·API.yml·DB.dbml 초안, 체크리스트, 센서 연결 원인 분석, 결정 15 |
| 2026-09-14 | 회사 컴퓨터 | HEADER | 하루 마감 — BE 7~9차·FRONT 보정 제거 결과 대조 확인, 설치 절차를 `.nosync` 방식으로, 결정 12·13 추가 |
| 2026-09-14 | 회사 컴퓨터 | FRONT | 보정 코드 2개 제거(`scenario`·`KEYBOARD_TILT_CONTROLS` → 서버 과정 설정), 깨진 node_modules 를 `deps.nosync` 로 재설치 |
| 2026-09-14 | 회사 컴퓨터 | BACK | BE 7~9차 — judgment endedAt, scenario 최상위, capAt:1, tiltControls, checkItems 순서, README 정리. venv 재구성 |
| 2026-09-14 | 회사 컴퓨터 | HEADER | 환경 구축, 이어받기·PROGRESS 옛 내용을 코드·DB 대조로 정리, 일정 정정(수업 9/15 시작) |
| 2026-09-14 | 개인 맥북 | FRONT·HEADER | 3D 장비 뷰 + 노광·현상 연출, 한글 입력 Q/E 버그 수정, 프롬프트 최신화 |
| 2026-09-13 | 개인 맥북 | BACK·FRONT·HEADER | 범용성 A·B·C(채점 데이터화·exercise_type·judgment), API명세서·발표자료 초안, 센서 수신 성공 |
| 2026-09-12 | 개인 맥북 | HEADER | 결정 9건 확정(입력 장치명·변환 위치·Web Serial·3축 매핑·시드 기준·버전 통일 등), 이어받기 파일 전면 갱신 |
| 2026-09-12 | 개인 맥북 | BACK | 시드를 프론트에 맞춤(8명/배정16/시도12), control 블록 추가, 과정 응답 평평화, 분석기 속도창 버그 수정 |
| 2026-09-12 | 개인 맥북 | BACK | 이상 탐지 시절 필드 전면 교체(orderOptionId / aligning·aligned / phase confirmed / events.axis) |
| 2026-09-12 | 개인 맥북 | HEADER | Front·Back 교차 확인 — 불일치 7건 발견, 세션별 작업 배정 |
| 2026-09-12 | 개인 맥북 | BACK | 백엔드 1차 — schema.sql, 규칙 기반 정렬 분석, FastAPI REST 10 + WS 1, LLM 골격 |
| 2026-09-12 | 개인 맥북 | FRONT | 정렬 실습으로 화면 전면 교체, 입력 어댑터 src/input/ 신설 |
| 2026-09-12 | 개인 맥북 | HEADER | 컨셉 전환(마스크·웨이퍼 정렬), 세션 체계 4개, DB설계서 초안 |
| 2026-09-12 | 개인 맥북 | HEADER | 깃 저장소 연결(jinsu1011/WME, 비공개), README 작성 |
| 2026-09-12 | 개인 맥북 | FRONT | 로그인·실습 목록·현재 상황 추가, 대기방 흐름으로 정리 (화면 11개) |
| 2026-09-12 | 개인 맥북 | HEADER | 포지셔닝·AI 역할·세션 분담 확정 |
| 2026-09-12 | 개인 맥북 | FRONT | 화면 9개 구현, 성취도 그래프 5종, 고객사 SK 하이닉수 적용 |
| 2026-09-12 | 개인 맥북 | HEADER | 프로젝트 방향 확정, 프론트 골격 생성 |
