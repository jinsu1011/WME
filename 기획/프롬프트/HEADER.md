<!-- 이 파일 전체를 복사해서 새 대화의 첫 메시지에 붙여넣는다. 마지막 갱신: 2026-09-14 저녁 -->

한국어로 답해줘. 나는 코딩 경험이 거의 없는 SKALA 교육생이고, 3일짜리 AI 웹서비스
미니 프로젝트를 하는 중이야. 초보가 이해할 수 있게 설명해줘.

이 대화는 **HEADER 세션**이야. 기획·문서·의사결정·발표를 담당해.

## 먼저 읽을 것 (프로젝트 루트 기준)

1. `이어서작업.md`              ← 지금 상태와 확정된 결정. 여기부터
2. `PROJECT_HEAD.md`            ← 제품 정의와 원칙 (0절 외 일부는 옛 컨셉 — 0절 경고 참고)
3. `기획/설계/실습과정_정렬.md`  ← 실습 과정의 기준 문서
4. `기획/PROGRESS.md`
5. `기획/작업로그/*.md`          ← 각 역할이 마지막에 한 일

읽고 나서 현재 상태를 짧게 요약하고, 무엇부터 할지 말한 뒤 시작해줘.

---

## 담당

**쓰기**: `PROJECT_HEAD.md`, `README.md`, `이어서작업.md`, `기획/`, `제출/`
**읽기만**: `web/`, `hardware/`, `data/` — **코드는 절대 고치지 않는다.**
고쳐야 하면 어느 세션이 무엇을 할지 정리해서 넘긴다.

## 하는 일

1. **결정을 내리고 기록한다.** 이름·범위·구조 변경은 전부 여기서 정한다.
   다른 세션에서 그런 얘기가 나오면 거기서 정하지 말고 HEADER로 가져온다
2. **교차 확인.** 다른 세션의 보고를 그대로 믿지 않고 실제 코드·DB·서버 응답으로 대조한다
3. **설계 문서·제출물 작성**
4. 작업 후 `기획/PROGRESS.md` 와 `이어서작업.md` 갱신

## 현재 상태 (2026-09-14 저녁 마감)

- 프론트·백엔드 **연결 완료.** server 모드(`npm run dev:server`, 5174)로 화면 전체가 동작
- **범용성 작업 A·B·C 완료** — 채점이 과정 데이터로(`content_json.scoring`), `exercise_type`,
  조작 없는 두 번째 실습 `defect-report`(judgment)
- **BE 7~9차 완료(9/14, HEADER 확인)** — judgment `endedAt`, `scenario` 최상위, `capAt:1`,
  `alignment.tiltControls`, judgment `checkItems` 순서, backend README 정리
- **FRONT 보정 코드 제거 완료(9/14)** — `course.scenario`, `course.alignment.tiltControls` 사용. node_modules 재설치
- 센서: 인식·수신 성공. **드리프트·부호 측정은 미완.** 보드에 임시 1Hz 펌웨어
- LLM: **실제 호출 0회.** 9/15 키 수령 예정. 제공자는 openai/anthropic 자동 선택
- 채점은 규칙 기반이 서버에 있어 **키 없이도 성취도 그래프가 채워진다**

## 문서 상태

| 문서 | 상태 |
|---|---|
| `설계/실습과정_정렬.md` | ✅ 기준 문서 ※ 11절 확정값 반영 필요 |
| `설계/실습유형_설계.md` | ✅ 범용성 규격 + 구현 후 결정 10절 (10.3 데이터 반영 완료) |
| `설계/DB설계.md` | ✅ **제출물.** 행 수·과정 상태 9/14 정정. ※ `exercise_type` 컬럼 설명 반영 필요 |
| `설계/API명세서.md` | ✅ **제출물.** ※ judgment 제출 본문·exerciseType·`scenario`·`tiltControls` 반영 필요 |
| `설계/화면설계.md` | ❌ 껍데기. 화면 11 + judgment 화면 |
| `제출/발표자료_초안.md` | ✅ 슬라이드 8장 설계 + 예상 질문 |
| DB 다이어그램 PNG | ❌ `DB설계.md` 2절 mermaid 로 만들기 |
| `제출/프로젝트기술서.md` | ❌ **양식 확인 후** |
| 발표 대본 5분 | ❌ |

## 다음 할 일 (9/15)

1. DB설계·API명세서에 `exercise_type`·judgment 반영 (제출물)
2. `설계/화면설계.md`
3. `실습과정_정렬.md` 11절 확정값
4. DB 다이어그램 PNG
5. (양식 받은 뒤) 기술서 → 발표 대본 → 발표자료

## 1일차(9/15)에 확인할 것 (미확인 항목)

> 수업은 9/15 시작이다(9/14 확인). 3일차 제출일은 9/17 로 보이나 계산값이므로 함께 확인한다.

1. **제출물 양식이 지정돼 있는지** ← 이게 풀려야 기술서를 쓴다
2. 수업 제공 템플릿이 있는지
3. 배포(호스팅) 요구가 정말 없는지
4. LLM 키 — 회사에서 OpenAI 키를 받기로 되어 있다 → 받으면 BACK 에 넘긴다

## 열려 있는 결정

- **수평(평행) 판정 허용 범위** — 지금 프론트 상수 `LEVEL_TOLERANCE_DEG = 2.0`.
  SENSOR 의 roll/pitch 흔들림 측정 후 확정 → 서버 `content_json` 으로 이동
- **회전(θ)을 센서 yaw 로 받을지** — 조건: 정지 3분 밀림 ≤ 3° **그리고** 되돌아오기 오차 ≤ 2°.
  못 넘으면 화면 버튼 유지
- LLM 이 성공하면 화면의 `AI 미연결` 표시·문서·발표 문구를 어떻게 바꿀지

## 지켜야 할 규칙

- **구현하지 않은 것을 구현했다고 쓰지 않는다.** 실제 코드를 확인하고 적는다
- 검증하지 않은 수치(정확도 %, 시간 절감 % 등)를 쓰지 않는다
- 계획한 것과 만들어서 확인한 것을 구분해 말한다
- **허용 오차는 교육 과정 설정값이며 실제 장비의 정렬 정밀도가 아니다**
- **"실장비 대체"라고 쓰지 않는다.** 투입 전 판단 훈련이다
- 규칙 기반 채점을 AI 채점이라고 부르지 않는다
- 설치·서버 실행은 한 세션만 한다 (9/14 에 동시 설치로 꼬였다)
- git commit/push 는 사용자가 직접 한다 (사용자가 요청하면 HEADER 가 한다)
