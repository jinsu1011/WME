# 이어서 작업하기 — WME

> **다른 노트북에서, 또는 새 대화에서 작업을 이어받을 때 이 파일부터 연다.**
> 대화 기록은 노트북 사이로 옮겨지지 않는다. 옮겨지는 건 이 폴더의 파일뿐이다.
> 그래서 **작업을 끝낼 때마다 이 파일을 갱신**해야 다음 사람(=다음 대화)이 이어받을 수 있다.

---

## 1단계 — 새 노트북에서 처음 시작할 때만

저장소: **https://github.com/jinsu1011/WME** (비공개)

```bash
git clone https://github.com/jinsu1011/WME.git
cd WME
npm --prefix web/frontend install
```

`node_modules` 는 깃에 올라가지 않으므로 노트북마다 따로 설치한다.

확인:

```bash
npm --prefix web/frontend run dev
```

http://localhost:5173 이 열리면 준비 완료다.

**데모 로그인**: 신입사원 `1` / `1`, 매니저 `2` / `2` (인증 구현이 아니라 데모 전환이다)

---

## 2단계 — 작업 시작 전에 항상

```bash
git pull
```

**다른 노트북에서 한 작업을 먼저 받아온다.** 이걸 빼먹으면 충돌이 난다.

---

## 3단계 — 역할 고르기

| 역할 | 할 일 | 지금 상태 |
|---|---|---|
| **Header** | 조율·결정·검증·통합 | 서비스명 WME 확정. 세션 5개 체계 정리 |
| **Front** | 화면 | 화면 11개. 이름 변경 + 연습 화면 알맹이 진행 중 |
| **Back** | API·DB·LLM 피드백 | 미착수 ← **지금 시작 가능** |
| **Sensor** | 펌웨어·데이터·이상 탐지 | 부품 미확보. **준비 작업은 지금 가능** |
| **계획** | 제출물 문서 작성 | DB설계 완료. API명세서 차례 |

---

## 4단계 — 해당 블록을 새 대화에 붙여넣기

### 🟦 Front — 화면

```
한국어로 답해줘. 나는 코딩 경험이 거의 없는 SKALA 교육생이고, 3일짜리 AI 웹서비스 미니 프로젝트를 하는 중이야.
이 대화는 Front 세션이야. web/frontend 폴더의 화면만 담당해.

시작 전에 이 파일들을 읽어:
1. PROJECT_HEAD.md — 제품 정의와 지켜야 할 원칙
2. 기획/PROGRESS.md — 완료·진행 중·다음 작업
3. 기획/세션/FRONT.md — 내 역할의 상세 규칙과 다음 작업
4. 이어서작업.md — 지금 어디까지 왔는지

읽고 나서, 무엇을 어떤 순서로 할지 짧게 말하고 시작해줘.
내 담당 폴더(web/frontend) 밖은 읽기만 하고 고치지 마.
작업이 끝나면 기획/세션/로그/<내 세션 이름>.md 맨 위에 한 줄 추가해줘. 다른 문서는 건드리지 마.
```

### 🟩 Back — API · DB · LLM

```
한국어로 답해줘. 나는 코딩 경험이 거의 없는 SKALA 교육생이고, 3일짜리 AI 웹서비스 미니 프로젝트를 하는 중이야.
이 대화는 Back 세션이야. web/backend 의 서버·DB·LLM 연결을 담당해.

시작 전에 이 파일들을 읽어:
1. PROJECT_HEAD.md — 제품 정의와 지켜야 할 원칙
2. 기획/PROGRESS.md — 완료·진행 중·다음 작업
3. 기획/세션/BACK.md — 내 역할의 상세 규칙과 작업 순서
4. web/frontend/src/types/index.ts — 사실상의 DB 스키마
5. web/frontend/src/api/index.ts — 내가 만들 엔드포인트와 1:1인 함수들

읽고 나서, 무엇을 어떤 순서로 할지 짧게 말하고 시작해줘.
프론트엔드 파일은 고치지 마. 고쳐야 하면 나에게 말해줘.
작업이 끝나면 기획/세션/로그/<내 세션 이름>.md 맨 위에 한 줄 추가해줘. 다른 문서는 건드리지 마.
```

### 🟧 Sensor — 펌웨어 · 데이터 · 이상 탐지

```
한국어로 답해줘. 나는 코딩 경험이 거의 없는 SKALA 교육생이고, 3일짜리 AI 웹서비스 미니 프로젝트를 하는 중이야.
이 대화는 Sensor 세션이야. hardware/ 와 data/ 의 센서 장치와 이상 탐지 모델을 담당해.

시작 전에 이 파일들을 읽어:
1. PROJECT_HEAD.md — 제품 정의와 지켜야 할 원칙
2. 기획/PROGRESS.md — 완료·진행 중·다음 작업
3. 기획/세션/SENSOR.md — 내 역할의 상세 규칙과 작업 순서
4. hardware/README.md, data/README.md

읽고 나서, 무엇을 어떤 순서로 할지 짧게 말하고 시작해줘.
내 담당 폴더 밖은 읽기만 하고 고치지 마.
작업이 끝나면 기획/세션/로그/<내 세션 이름>.md 맨 위에 한 줄 추가해줘. 다른 문서는 건드리지 마.
```

### 🟪 Header — 조율 · 결정 · 검증

```
한국어로 답해줘. 나는 코딩 경험이 거의 없는 SKALA 교육생이고, 3일짜리 AI 웹서비스 미니 프로젝트를 하는 중이야.
이 대화는 Header 세션이야. 기획·문서·의사결정·발표를 담당해.

시작 전에 이 파일들을 읽어:
1. PROJECT_HEAD.md — 제품 정의와 지켜야 할 원칙
2. 기획/PROGRESS.md — 완료·진행 중·다음 작업
3. 기획/세션/HEADER.md — 내 역할의 상세 규칙
4. 이어서작업.md — 지금 어디까지 왔는지

읽고 나서, 지금 상태를 짧게 요약하고 다음에 할 일을 제안해줘.
코드는 직접 고치지 마. 고쳐야 하면 어느 세션에서 뭘 해야 하는지 정리해줘.
문서에 구현하지 않은 것을 구현했다고 쓰지 마. 실제 코드를 확인하고 적어줘.
```

---

### 🟨 계획 — 제출물 문서

```
한국어로 답해줘. 나는 코딩 경험이 거의 없는 SKALA 교육생이고, 3일짜리 AI 웹서비스 미니 프로젝트를 하는 중이야.
이 대화는 계획 세션이야. 제출물 문서 작성을 담당해. 서비스명은 WME — We Make Experts 로 확정됐어.

시작 전에 이 파일들을 읽어:
1. PROJECT_HEAD.md — 제품 정의와 포지셔닝
2. 기획/PROGRESS.md — 완료·진행 중·다음 작업
3. 기획/세션/계획.md — 내 역할의 상세 규칙과 작업 순서
4. 기획/설계/DB설계.md — 이미 완료된 문서. 같은 밀도와 형식으로 맞춘다

읽고 나서, 무엇을 어떤 순서로 할지 짧게 말하고 시작해줘.
코드는 고치지 마. 기획/설계/ 와 제출/ 에만 쓴다.
작업이 끝나면 기획/세션/로그/계획.md 맨 위에 한 줄 추가해줘.
```

---

## 5단계 — 작업을 끝낼 때 (중요)

노트북을 바꾸거나 대화를 닫기 전에 **반드시** 아래 세 가지를 한다. 이게 유일한 인수인계 수단이다.

1. **`기획/세션/로그/<내 세션>.md`** 맨 위에 한 줄 추가 — 각 세션이 자기 파일만 쓰므로 충돌하지 않는다
2. Header 세션이 그 로그들을 모아 `기획/PROGRESS.md` 와 이 파일을 갱신한다
3. 깃에 올리기

```bash
git add -A
git commit -m "무엇을 했는지 한 줄"
git push
```

### 깃 규칙은 두 개뿐

- **시작할 때 `git pull`, 끝낼 때 `commit` + `push`**
- **두 노트북에서 동시에 작업하지 않는다**

이 둘만 지키면 충돌이 날 일이 거의 없다.
뭔가 잘못돼도 `git log` 로 되돌릴 수 있으니, 겁내지 말고 자주 커밋한다.

---

## 현재 상태

> 마지막 갱신: 2026-09-12 / 개인 맥북 / HEADER

### 전체

기획 문서 완료. 프론트엔드 화면 **11개** 구현 완료(로그인 → 실습 목록 → 대기방 → 연습 흐름, 시드 데이터 기반).
백엔드·센서·LLM 미착수. 서비스명 **WME (We Make Experts)** 확정.

용어: 화면에서는 **신입사원 / 매니저** 를 쓴다.

### 역할별

**FRONT** — 화면 11개 완료. 타입 검사·빌드·린트 통과.
`/login`, `/learn`(실습 목록), `/learn/status`(현재 상황), `/learn/courses`, `/learn/records`,
`/courses/:id`(대기방), `/attempts/new`(연습), `/attempts/:id/result`,
`/instructor`, `/instructor/learners`, `/instructor/learners/:id`

**다음 (가장 중요)**: 연습 화면 `/attempts/new` 의 알맹이. `src/sensor/` 폴더가 아직 비어 있다.
mock 센서 어댑터 → 실시간 차트 → 단계 버튼 → 이상 후보 구간 → 근거 선택 → 답변 제출 순서.
상세는 `기획/세션/FRONT.md`.

**BACK** — 폴더와 README만 있음. 코드 없음.
다음: `web/backend/schema.sql` (6테이블 DDL을 `src/types/index.ts` 에서 생성) → FastAPI 골격 →
LLM 피드백. **FRONT의 답변 제출이 되고 나서 시작하는 게 순서다.**

**SENSOR** — 부품 미확보. 보드 모델·MPU6050 보유 여부·납땜 필요 여부 전부 미확인.
센서가 없어도 프로젝트는 완성된다. 서두르지 않는다.

**HEADER** — 포지셔닝·AI 역할·세션 분담·발표 구성 확정. 설계 문서 3종은 목차만.
다음: `기획/설계/` 3종 작성.

### 지금 막혀 있는 것

- LLM API 키 확보 경로 — 1일차에 수업 지급 여부 확인
- 센서 부품 미확보

---

## 작업 로그

새로 추가할 때 **맨 위에** 적는다. 한 줄이면 된다.

| 날짜 | 노트북 | 역할 | 한 일 |
|---|---|---|---|
| 2026-09-12 | 개인 맥북 | HEADER | 깃 저장소 연결(jinsu1011/WME, 비공개), README 작성 |
| 2026-09-12 | 개인 맥북 | HEADER | 이어받기 파일 생성, 실제 구현 상태에 맞춰 문서 동기화 |
| 2026-09-12 | 개인 맥북 | FRONT | 로그인·실습 목록·현재 상황 추가, 대기방 흐름으로 정리 (화면 11개) |
| 2026-09-12 | 개인 맥북 | HEADER | 포지셔닝·AI 역할·세션 분담 확정 |
| 2026-09-12 | 개인 맥북 | HEADER | 폴더 구조 정리, 설계 문서 목차, 제출 체크리스트 |
| 2026-09-12 | 개인 맥북 | FRONT | 화면 9개 구현, 성취도 그래프 5종, 고객사 SK 하이닉수 적용 |
| 2026-09-12 | 개인 맥북 | HEADER | 프로젝트 방향 확정, 프론트 골격 생성 |
