# 이어서 작업하기 — WME

> **다른 노트북에서, 또는 새 대화에서 작업을 이어받을 때 이 파일부터 연다.**
> 대화 기록은 노트북 사이로 옮겨지지 않는다. 옮겨지는 건 이 폴더의 파일뿐이다.
> 그래서 **작업을 끝낼 때마다 이 파일을 갱신**해야 다음 사람(=다음 대화)이 이어받을 수 있다.
>
> 마지막 갱신: **2026-09-14 / 회사 컴퓨터 / HEADER**

---

## 1단계 — 새 컴퓨터에서 처음 시작할 때만

저장소: **https://github.com/jinsu1011/WME** (비공개 — GitHub 로그인이 필요하다)

```bash
git clone https://github.com/jinsu1011/WME.git
cd WME
```

### (1) 화면 쪽 설치

```bash
npm --prefix web/frontend install
```

### (2) 서버 쪽 설치

```bash
cd web/backend
python3 -m venv .venv
./.venv/bin/pip install -r requirements.txt
```

> ⚠️ **venv 는 노트북마다 새로 만든다.** iCloud 로 따라온 `.venv` 는 다른 노트북의 파이썬 경로
> (`/Users/kimjinsu`, `/opt/anaconda3` 등)를 가리켜 실행되지 않는다. 옮겨 온 걸 고쳐 쓰지 말고 새로 만든다.
>
> ⚠️ **프로젝트가 iCloud 폴더 안에 있으면** `.venv/bin/python` 링크가 사라져
> `no such file or directory: .venv/bin/python` 이 날 수 있다. 그때는 동기화 제외 폴더에 만들고 링크를 건다
> (회사 컴퓨터에서 이 방식으로 동작 확인, 2026-09-14):
>
> ```bash
> cd web/backend
> rm -rf .venv
> python3 -m venv .venv.nosync
> ln -s .venv.nosync .venv
> ./.venv/bin/pip install -r requirements.txt
> ```

### (3) ⚠️ DB 만들기 — **이걸 빼먹으면 화면이 텅 빈다**

`data/local/*.db` 는 **깃에 올라가지 않는다.** 새 컴퓨터에는 DB 파일이 없으므로 직접 만든다.

```bash
cd web/backend
./.venv/bin/python -m app.seed
```

`시드 완료: 학습자 8명, 정렬 시도 12건 + 판단 시도 2건` 이 나오면 성공이다.
DB 파일은 **저장소 루트의** `data/local/wme.db` 에 생긴다.

### (4) 실행 — 창 두 개

```bash
cd web/backend && ./.venv/bin/python -m uvicorn app.main:app --port 8000
```

```bash
npm --prefix web/frontend run dev:server
```

**http://localhost:5174** 이 열리면 준비 완료다 (서버 데이터를 쓰는 모드).

- `npm --prefix web/frontend run dev` 는 **mock 모드(5173)** 다. 서버 없이 시드로 돈다 — 발표장 백업용
- `--reload` 를 쓰지 않는다. iCloud 동기화 폴더에서는 2분마다 죽는다

**데모 로그인**: 신입사원 `1` / `1` (김진수), 매니저 `2` / `2` (최태원)
— 인증 구현이 아니라 데모 전환이다.

**센서를 쓰려면 크롬이어야 한다.** Web Serial 은 크롬 계열에서만 동작한다.

### (5) LLM 키 (있으면)

`web/backend/.env` 를 열어 키를 넣는다. **줄바꿈 없이 따옴표 안에만.**
이 파일은 깃에 올라가지 않는다.

```bash
cd web/backend && source .env && ./.venv/bin/python -m uvicorn app.main:app --port 8000
```

`http://localhost:8000/api/health` 의 `llmConfigured` 가 `true` 면 들어간 것이다.

## 2단계 — 작업 시작 전에 항상

```bash
git pull
```

**다른 노트북에서 한 작업을 먼저 받아온다.** 이걸 빼먹으면 충돌이 난다.

---

## 3단계 — 지금 어디까지 왔나 (2026-09-14 기준, 코드·DB 대조)

### 한 줄 요약

**구현은 거의 끝났다.** 프론트·백엔드가 연결돼 있고, 정렬 실습은 3D 장비 뷰에서
조작 → 확정 → 노광 → 현상 → 오차만큼 어긋난 패턴까지 보여준다.
**남은 건 센서 실측, LLM 실제 호출, 제출 문서다.**

### 일정 (착각 주의)

```
9/14 (오늘)  사전 작업 — 수업은 내일 시작. 제출물 양식 파일은 아직 못 받음
9/15 1일차   ← 제출물 양식·템플릿·배포 요구·LLM 키를 여기서 확인
9/16 2일차
9/17 3일차   13:00~14:00 제출 + 발표   ※ "9/15 시작 · 3일 과정" 에서 계산한 날짜. 1일차에 확인
```

**쫓기는 게 아니라 앞서 있다.** 수업이 시작도 안 했는데 구현이 거의 끝났다.
대신 **1일차에 요건이 바뀔 수 있으므로, 양식을 타는 문서(기술서)는 그 뒤에 쓴다.**

### 역할별 상태

| 역할 | 다음 할 일 |
|---|---|
| **Header** | DB설계·API명세서에 exercise_type·judgment 반영 → `화면설계.md` → `PROJECT_HEAD.md` 옛 상태 정리 → 기술서(양식 받은 뒤) → 발표 대본 |
| **Front** | **실물 센서 브라우저 수신 확인**(아직 아무도 못 함), BACK 수정 후 보정 코드 제거 |
| **Back** | judgment `checkItems` 순서 바꾸기(설계서 10.3), **LLM 실제 호출**. (endedAt·scenario·capAt·tiltControls 는 9/14 BE 7차 완료·확인) |
| **Sensor** | **드리프트 3분 → 되돌아오기 6회 → 부호 → 모형 조립** (pitch 60° 현상은 재현 안 됨) |

### 구현된 것 (실제로 돌려서 확인한 것만)

**프론트** `web/frontend`
- React + Vite + TypeScript + Tailwind v4 + Recharts + React Router + **Three.js**
- 화면 11개(라우트). 실습 2종 — `photo-align`(alignment) / `defect-report`(judgment, 조작 없음)
- **mock / server 전환** — `npm run dev`(mock, 5173) / `npm run dev:server`(server, 5174).
  **mock 은 발표 백업이라 지우지 않는다**
- **3D 장비 뷰** (`src/three/alignmentScene.ts`) — 스테이지 + 웨이퍼 + 마스크 판.
  마우스로 잡고 끌면 시점 회전(줌·팬은 끔). 조작이 리액트 상태를 안 거치고 즉시 반영
- **정렬 확정 → 노광 → 현상액 낙하 → 퍼짐 → 패턴이 오차만큼 어긋나 찍힘.**
- 조작: 방향키 X/Y · Q,E 회전 · W,A,S,D 기울기 · Shift 미세조정.
  **키는 물리 위치(`event.code`)로 판정** — 한글 입력 상태에서도 동작한다
- 입력 어댑터 `src/input/` — Keyboard / Tilt(Web Serial 직결)
- WebGL 을 못 쓰면 2D 현미경 뷰로 자동 대체

**백엔드** `web/backend`
- `schema.sql` 6테이블 (users / courses / enrollments / attempts / measurements / events) + `courses.exercise_type`
- `main.py` **REST 15개 + WebSocket 1개**
- `app/analyzers/` — alignment(규칙 기반 정렬 경로 분석, **모델 학습 없음**) / judgment
- 채점 규칙은 과정 데이터(`content_json.scoring`), `scoring.py` 는 규칙 해석기
- `llm/` 입력 구성·출력 스키마·응답 검증·재시도, 제공자 자동 선택(anthropic/openai).
  **실제 호출 0회.** 가짜 응답으로만 검증했다
- DB `data/local/wme.db` — 학습자 8명 + 담당자 1명, 배정 17건, 과정 5개(실습 가능 2), 시도 14건(정렬 12 + 판단 2)

**센서** `hardware/`
- Arduino UNO + MPU6050 인식, 50Hz `WME,` 샘플 수신 (시리얼 모니터 기준)

### 아직 안 된 것 (오해 방지)

- **LLM 실제 호출** — 한 번도 못 했다. 피드백은 규칙 기반 샘플이며 화면에 그렇게 표시한다
- **센서 실측** — 드리프트·부호·되돌아오기 미측정. 브라우저에서 실센서로 실습해 본 적 없음
- **제출물** — DB설계·API명세서 원본은 있으나 exercise_type 반영 필요. 화면설계는 비어 있음.
  기술서·발표 대본·PDF 없음

---

## 3.5단계 — HEADER 확정 결정 (2026-09-12 ~ 13)

새 대화에서 이걸 다시 논의하지 않는다. 이미 정해진 것이다.

| # | 결정 | 이유 |
|---|---|---|
| 1 | 과정 ID는 **`photo-align`** | 프론트·서버 양쪽 일치 확인 완료 |
| 2 | 입력 장치 필드는 **`inputDevice` / `keyboard`·`model_controller`** (서버 이름 기준) | DB 컬럼명이 제출물에 박히므로 서버를 기준으로 잡는다 |
| 3 | **기울기 → 이동 변환은 프론트가 계산**한다 | 조작은 즉시 반응해야 한다. 서버 왕복하면 조작감이 무너진다 |
| 4 | 변환 계수·허용 오차는 **과정 설정값**(`courses.content_json`)으로 둔다 | 코드에 박은 상수가 아니라 과정 설정이어야 설명이 된다 |
| 5 | 센서 데이터 경로는 **Web Serial API** (아두이노 → 브라우저 직통) | 서버를 거치지 않아 단순하다. 발표는 크롬으로 한다 |
| 6 | 모형 3축 매핑: **roll→X, pitch→Y, yaw→θ, 전부 속도 방식** | 센서가 잴 수 있는 건 회전 3축뿐. 절대 매핑이면 모형을 내려놓는 순간 정렬이 풀린다 |
| 7 | 서버 시드를 **프론트 시드에 맞춘다** (8명/12건) | 담당자 화면이 학습자 1명이면 제품 가치가 안 보인다 |
| 8 | 프론트에 **`VITE_API_MODE=mock\|server`** 스위치를 둔다 | 발표장에서 서버가 안 뜨면 mock 으로 되돌아간다 |
| 9 | 버전 문자열은 **`course-2.0.0`** 으로 통일 | 시도마다 복사 저장되는 값이라 두 개가 돌아다니면 추적이 무의미해진다 |
| 10 | **실습 화면 중앙에 3D 장비 뷰**(Three.js). 현미경 뷰(2D 마크)는 옆으로 | 실제 얼라이너가 스테이지+현미경 구성이다. 현미경 뷰만 있으면 "마크 겹치기 게임"으로 보인다 |
| 11 | 범용성: 채점을 과정 데이터로, `exercise_type` 도입, 조작 없는 `judgment` 실습 추가 | "데이터만 갈아끼우면 된다"를 말이 아니라 증명으로 |

> **결정 10은 이전 지시(“3D 라이브러리 쓰지 마라, CSS 로, 옆에 작게”)를 뒤집은 것이다.**
> 그 지시로 나온 작은 원판이 조잡해서 바꿨다. Three.js 는 로컬 Vite 앱이라 CDN 제약이 없다.
> 단 **포토리얼은 목표가 아니고**, WebGL 이 안 되면 기존 CSS 패널로 자동 대체한다.

> **결정 6 이후 센서 모드 조작이 바뀌었다(2026-09-13).** 센서 모드에서는 기울기 = 수평(평행) 맞추기,
> yaw = 회전 θ, X/Y = 키보드 방향키. 근거는 접촉식 얼라이너의 실제 순서(평행 → 회전·위치).

**확정값**
- 허용 오차: 위치 **±4px**, 회전 **±1.0°** — *교육 과정 설정값이며 실제 장비의 정렬 정밀도가 아니다*
- 조작 계수: `deadZoneDeg 2.0` / `gainPxPerDeg 12.0` / `maxSpeedPx 160.0` /
  `yawDeadZoneDeg 3.0` / `yawGainDegPerDeg 2.5` / `maxSpeedDeg 30.0` /
  키보드 `movePxPerSec 90.0`, `rotateDegPerSec 22.0`, `fineFactor 0.25`
- 버전: `courseVersion course-2.0.0` / `modelVersion rule-align-1.0.0` /
  `settingsVersion align-settings-1.0.0`

**아직 안 정한 것**
- **수평 판정 허용 범위** — 지금 프론트 상수 ±2.0°. 센서 흔들림 측정 후 확정 → 서버 `content_json` 으로
- **회전(θ)을 센서 yaw 로 할지** — 정지 3분 밀림 ≤ 3° **그리고** 되돌아오기 오차 ≤ 2° 면 채택, 아니면 화면 버튼·키보드

---

## 4단계 — 역할 프롬프트를 새 대화에 붙여넣기

**프롬프트는 `기획/프롬프트/` 폴더에 있다.** 파일 하나를 통째로 복사해서
새 대화(Claude / Codex 등)의 첫 메시지에 붙여넣으면 그 역할로 시작한다.

| 파일 | 언제 쓰나 | 건드리는 폴더 |
|---|---|---|
| `기획/프롬프트/HEADER.md` | 문서·발표자료, 결정이 필요할 때 | `PROJECT_HEAD.md`, `기획/`, `제출/` |
| `기획/프롬프트/FRONT.md` | 화면 | `web/frontend/` |
| `기획/프롬프트/BACK.md` | 서버·DB·LLM | `web/backend/`, `data/local/` |
| `기획/프롬프트/SENSOR.md` | 아두이노·측정 | `hardware/`, `data/` |

각 파일에 그 역할의 실행 명령, 구현 현황, 남은 일, 지켜야 할 규칙이 전부 들어 있다.
**프롬프트를 이 파일에 중복해 두지 않는다.** 두 곳에 있으면 반드시 서로 달라진다.

규칙 두 개만 지키면 사고가 안 난다.

1. **폴더 소유권** — 남의 폴더는 읽기만 한다
2. **결정은 HEADER에서만** — 이름·범위·구조 변경이 다른 대화에서 나오면
   거기서 정하지 말고 HEADER로 가져온다

## 5단계 — 작업을 끝낼 때 (중요)

노트북을 바꾸거나 대화를 닫기 전에 **반드시** 아래 세 가지를 한다. 이게 유일한 인수인계 수단이다.

1. **`기획/작업로그/<내 세션>.md`** 맨 위에 한 줄 추가 — 각 세션이 자기 파일만 쓰므로 충돌하지 않는다
2. Header 세션이 그 로그들을 모아 `기획/PROGRESS.md` 와 이 파일을 갱신한다
3. 깃에 올리기

```bash
git add -A
git commit -m "무엇을 했는지 한 줄"
git push
```

### 깃 규칙은 두 개뿐

- **시작할 때 `git pull`, 끝낼 때 `commit` + `push`**
- **두 노트북에서 동시에 작업하지 않는다**

이 둘만 지키면 충돌이 날 일이 거의 없다.
뭔가 잘못돼도 `git log` 로 되돌릴 수 있으니, 겁내지 말고 자주 커밋한다.

---

## 지금 막혀 있는 것

- **제출물 양식·템플릿** — 아직 파일을 못 받았다(9/14). 1일차(9/15)에 확인
- **LLM API 키** — 회사에서 OpenAI 키를 받기로 했다. 키가 없으면 발표에서 이렇게 말한다:
  "LLM 피드백은 입력 구성·출력 스키마·응답 검증·재시도까지 구현했고 API 키 연결만 남았습니다.
  현재 화면의 피드백은 규칙 기반 샘플이며 화면에 그렇게 표시하고 있습니다."

---

## 작업 로그

새로 추가할 때 **맨 위에** 적는다. 한 줄이면 된다.
세션별 자세한 기록은 `기획/작업로그/` 에 있다.

| 날짜 | 노트북 | 역할 | 한 일 |
|---|---|---|---|
| 2026-09-14 | 회사 컴퓨터 | HEADER | 회사 컴퓨터 환경 구축(iCloud venv 링크 문제 우회), 이어받기·PROGRESS 옛 내용을 코드·DB 대조로 정리, 일정 정정(수업 9/15 시작) |
| 2026-09-14 | 개인 맥북 | FRONT·HEADER | 3D 장비 뷰 + 노광·현상 연출, 한글 입력 Q/E 버그 수정, 프롬프트 최신화 |
| 2026-09-13 | 개인 맥북 | BACK·FRONT·HEADER | 범용성 A·B·C(채점 데이터화·exercise_type·judgment), API명세서·발표자료 초안, 센서 수신 성공 |
| 2026-09-12 | 개인 맥북 | HEADER | 결정 9건 확정(입력 장치명·변환 위치·Web Serial·3축 매핑·시드 기준·버전 통일 등), 이어받기 파일 전면 갱신 |
| 2026-09-12 | 개인 맥북 | BACK | 시드를 프론트에 맞춤(8명/배정16/시도12), control 블록 추가, 과정 응답 평평화, 분석기 속도창 버그 수정 |
| 2026-09-12 | 개인 맥북 | BACK | 이상 탐지 시절 필드 전면 교체(orderOptionId / aligning·aligned / phase confirmed / events.axis) |
| 2026-09-12 | 개인 맥북 | HEADER | Front·Back 교차 확인 — 불일치 7건 발견, 세션별 작업 배정 |
| 2026-09-12 | 개인 맥북 | BACK | 백엔드 1차 — schema.sql, 규칙 기반 정렬 분석, FastAPI REST 10 + WS 1, LLM 골격 |
| 2026-09-12 | 개인 맥북 | FRONT | 정렬 실습으로 화면 전면 교체, 입력 어댑터 src/input/ 신설 |
| 2026-09-12 | 개인 맥북 | HEADER | 컨셉 전환(마스크·웨이퍼 정렬), 세션 체계 4개, DB설계서 초안 |
| 2026-09-12 | 개인 맥북 | HEADER | 깃 저장소 연결(jinsu1011/WME, 비공개), README 작성 |
| 2026-09-12 | 개인 맥북 | FRONT | 로그인·실습 목록·현재 상황 추가, 대기방 흐름으로 정리 (화면 11개) |
| 2026-09-12 | 개인 맥북 | HEADER | 포지셔닝·AI 역할·세션 분담 확정 |
| 2026-09-12 | 개인 맥북 | FRONT | 화면 9개 구현, 성취도 그래프 5종, 고객사 SK 하이닉수 적용 |
| 2026-09-12 | 개인 맥북 | HEADER | 프로젝트 방향 확정, 프론트 골격 생성 |
